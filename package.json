{
  "name": "sample-node-api",
  "version": "1.0.0",
  "private": true,
  "description": "Sample Node API for SIT223/SIT753 7.3HD Jenkins pipeline demo",
  "scripts": {
    "start": "node server.js",
    "build": "echo \"no-op build\"",
    "test": "jest --runInBand --coverage --reporters=default --reporters=jest-junit"
  },
  "dependencies": {
    "express": "^4.19.2",
    "prom-client": "^15.1.3",
    "lodash": "^4.17.15"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "supertest": "^7.0.0"
  }
}